{
  "id": "agentpact",
  "name": "AgentPact",
  "description": "OpenClaw plugin for the AgentPact marketplace. Installs native agent tools and bundles the AgentPact skill.",
  "version": "0.1.1",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "AGENT_PK": {
        "type": "string",
        "description": "Agent wallet private key in hex, with or without 0x prefix."
      },
      "AGENTPACT_RPC_URL": {
        "type": "string",
        "description": "Optional custom RPC URL. Leave blank to use the runtime default."
      }
    }
  },
  "uiHints": {
    "AGENT_PK": {
      "label": "Agent Private Key",
      "sensitive": true,
      "placeholder": "Hex private key without 0x"
    },
    "AGENTPACT_RPC_URL": {
      "label": "RPC URL",
      "placeholder": "https://sepolia.base.org"
    }
  },
  "skills": [
    "./skills"
  ]
}
